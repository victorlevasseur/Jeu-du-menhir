language: java

addons:
    apt:
        sources:
            - deadsnakes
            - ubuntu-toolchain-r-test
        packages:
            - eclipse
        
install:
    - cd JeuDuMenhir
    - ant deps
script:
    - cd JeuDuMenhir
    - ant MainJoueurTest
    
